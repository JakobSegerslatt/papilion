import { expect } from 'chai';
import 'mocha';

import {
    Storage
} from '../src/index';

describe('@Storage test', () => {
    it('Should store a value in sessionStorage', () => {
        // @Storage()
        // const token = 'Store value';
        // const fromSession = sessionStorage.getItem('token');
        // expect(fromSession).to.equal('Store value');
    });
});
